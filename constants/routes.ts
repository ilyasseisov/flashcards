const ROUTES = {
  // HOME: '/',
  // BLOG: '/blog',
  // POST: (slug: string) => `/blog/${slug}`,
};

export default ROUTES;

// usage
// <Link href={ROUTES.HOME}>
// <Link href={ROUTES.POST(post.slug)}>
